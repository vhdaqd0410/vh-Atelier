# 依赖与运行环境（不入库，随部署包分发）
node_modules/
runtime/
engine/
models/
bin/*.exe
bin/whisper/
bin/sherpa/
ncm/node_modules/

# 运行时用户数据
collect/
*.log
*.pyc
__pycache__/
_tmp/
_releases/

# 隐私数据（绝不入库）
.video_cookie
.video_cookies.txt
*.cookies.txt
_enhance_token.json
feedback.json
collect/feedback.json

# 但配置模板要入库（供别人参考填写）
!feedback.example.json

# 系统
Thumbs.db
desktop.ini
.DS_Store

# 短剧扒歌：设备号是运行时生成的本机标识，不入库
py/hongguo_device.json
# 短剧扒歌：批量结果落盘目录（运行时数据）
collect/bgm/
# 短剧扒歌：下载与转码缓存
collect/video/
