# Python
__pycache__/
*.pyc
*.pyo

# 大模型 / 引擎（不随仓库分发，运行时回退到旧字幕插件 CEP 目录或本机部署）
models/*.bin
models/funasr/
models/spleeter/
bin/whisper/
bin/sherpa/

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store

# 编译产物（源码 keyhook/Program.cs 入仓，exe 不入仓）
keyhook/*.exe

# 网易云音乐服务依赖（ncm/node_modules 不入仓，部署时 npm install）
ncm/node_modules/
